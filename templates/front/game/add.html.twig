{% extends 'front/_layout/_base.html.twig' %}
{% form_theme form 'front/_layout/form-theme.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}
    <div class="strip backgrounded img_forest screenEmphasis">
        <div class="widget">
            <h1>Create a game</h1>
            <form method="post" action="" data-form-checker="true">
                {{ form_start(form) }}
                {{ form_row(form.name) }}
                {{ form_row(form.description) }}
                {{ form_row(form.background) }}
                {{ form_row(form.isActive) }}
                <input type="submit" value="Create game" />
                {{ form_end(form) }}
            </form>
        </div>
    </div>
{% endblock %}